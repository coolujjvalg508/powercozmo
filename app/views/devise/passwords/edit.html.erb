<section id="md" class="inr-pg">
  <div class="container log-block">
    <div class="row">
      <div class="col-lg-6 log-lt">
        <div class="log-lt-cont">
          <h2>Need to sell equipment?</h2>
          <p>Whether your company is big or small, Power Cozmo. <br>
            can sell your power equipment or plants quickly, <br>
            easily, and for the best returns possible.</p>
        </div>
        <img src="/assets/log-img.jpg" alt="" class="img-responsive"> <span class="log-img-small"><img src="/assets/log-img-small.png" alt=""></span> </div>
      <div class="col-lg-6 log-rt">
        <div class="log-rt-cont">
          <h2 class="page-hd txtctr">Change Password</span></h2>
          <div class="log-mid-box">
          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, id: 'change_password' }) do |f| %>

            <%= f.hidden_field :reset_password_token, as: :hidden %>

          	<div class="form-group"> <i class="fa fa-lock"></i>
              <%= f.password_field :password, autocomplete: "off", placeholder: "Enter Password*", required: true, autofocus: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
              <span class="err-msg"><%= error_message(resource, :password)%></span>
            </div>
            <div class="form-group"><i class="fa fa-lock"></i>
              <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Enter Confirm Password*", required: true %>
              <span class="err-msg"><%= error_message(resource, :password_confirmation)%></span>
            </div>
					  <div class="form-group"><%= f.submit "Change Password", class: "log-btn" %>
            </div>
					<% end %>
          </div>
          <div class="cr-acco"> New to our site? <%= link_to('Create a free account', new_user_registration_path) %>. it's fast, free and <br>
            gives you full access to our website features. <%= link_to('Sign Up', new_user_registration_path, class: "sign-up-btn") %></div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  $(document).ready(function(){
    $("#change_password").validate({
      rules: {
        "user[password]": "required",
        "user[password_confirmation]": {
          required: true,
          equalTo: "#user_password"
        },
      },
      messages: {
        "user[password]": {
          required: "Password can't be blank"
        },
        "user[password_confirmation]": {
          required: "Password confirmation can't be blank",
          equalTo: "Password confirmation doesn't match"
        }
      },
      errorPlacement: function(error, element) {
        error.insertAfter(element);
      }
    });
  });
</script>